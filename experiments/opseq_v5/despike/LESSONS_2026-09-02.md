# LESSONS 2026-09-02 — armadillo: DMesh comparison, resolution, 6v limits

All numbers: ho16 = IoU over 16 held-out views (DMesh mv-recon exam), armadillo,
64 training views unless "6v". Figures/meshes in `results64v/`, `results6v/`.

## 1. Golden result (pure GenesisTopmod, 64v)
`p5_64_taubin5`: V 18,253 / F 36,502, watertight genus 0, **ho16 0.9957**,
SI faces 1.4 %, folds 0.2 %, back dihedral 7.9° (GT-decimated ref 10.1°).
Chain: icosphere 0.9279 → DLFL extrude carves 0.9572 → voting visual hull 0.9841
→ in-loop DLFL flip/collapse/push 0.9865 → global DLFL subdivision 0.9914 → Taubin×5 0.9957.
DMesh (official code, same exam): 0.9891 / 0.9894 (hi-res), non-manifold soup.

## 2. DMesh output resolution is locked by its representation
Every epoch, DMesh starts with the refine-seeded point set and its optimization
prunes >85 % of faces within 500 steps — with the real regularizer at 0 and reals
frozen to 1 every step it is the same (`armadillo_64v_dense_{a,b}.yaml`):

| run | epoch_3 start F | final F | ho16 |
|---|---|---|---|
| default | 15.6k | 4.8k | 0.9891 |
| hires (38k seeds) | 45.6k | 5.5k | 0.9894 |
| dense_a (no real reg, frozen reals) | 45.8k | 8.1k | 0.9889 |
| dense_b (+98k seeds, ud_thresh 1e-2) | **187.7k** | **9.2k** | 0.9885 |

Face existence comes from the weighted-Delaunay tets; denser points compete for
the same faces. More seeds ≠ more output faces. This is the method's ceiling,
not our config.

## 3. Ceiling per face count (GT quadric-decimated, same exam)
| faces | GT-decimated ceiling | DMesh | ours |
|---|---|---|---|
| 5.5k | 0.9897 | 0.9894 | 0.9921 (golden decimated) |
| 9.2k | 0.9934 | 0.9885 | 0.9946 (golden decimated) |
| 36.5k | 0.9985 | — | 0.9957 |
| 56.8k | ~0.999 | — | **0.9972** (`p6_50k_taubin5`, partial subdiv of 1500 largest faces) |
| 219k | 1.0000 | — | aborted: SI 32 % after 100 steps (edge 0.015 < 256² pixel) |

- DMesh at 5.5k sits AT its face-count ceiling — it is not a bad optimizer, it is
  resolution-locked.
- Ours at 36.5k is 0.0028 below ceiling: the residual is optimization error
  (1.4 % SI, local under-fit), not resolution. Going to 100k+ faces raises the
  ceiling by 0.0015 only; expected gain ≤ +0.001.
- Honest caveat: optimized DIRECTLY at 6.3k faces we get 0.9865 < DMesh 0.9894.
  Our margin comes from coarse-to-fine (subdivide → optimize → decimate keeps the
  detail), which DMesh cannot do.

## 4. 6v: information-limited, not resolution-limited
| mesh | V/F | ho16 | train | notes |
|---|---|---|---|---|
| pure 6v low-res `p4d_pure6` | 3.4k/6.8k | 0.9457 | — | |
| pure 6v hi-res + Taubin `p5_pure6_taubin5` | 20.4k/40.8k | 0.9555 | 0.9970 | overfit |
| hybrid (DMesh-ourinit soup → reverse pull) | — | 0.9703–0.9729 | | 5-loop plateau |

Error split by visibility from the 6 training views (hi-res mesh, |dist| to GT):
visible 66 % of verts: median 0.012; unseen 34 %: median 0.067, 52 % of them off
by > 2 edge lengths. 64v golden: 0.001 overall. The "floating triangles" look is
one watertight sheet (1 component) crumpling in unseen regions — error is at the
same scale as the edge length (0.033), so the fine mesh reads as wrinkled paper.

## 5. Supervision — NOT silhouette-only
Both we and DMesh use GT silhouette + GT depth (256², `W_DEPTH=0.35` + phase-in
ours / `depth_coef 1.0` DMesh) per training view; 64v adds diffuse shading.
DMesh 6v uses our exact 6 cameras (`use_our6`). Only the visual-hull field is
silhouette-only. No `W_DEPTH=0` ablation has been run on armadillo 6v.
Visible-surface error of 0.012 at 6v (vs 0.001 at 64v) means depth is
under-exploited: candidate fix = fuse the 6 depth maps into the hull field (TSDF
style "depth hull") so visible surfaces are pinned by the field.

## 6. Self-intersection lesson (from 09-01/02)
IoU is blind to self-intersections. Boss spotted "too smooth to be ours" →
audit showed 46–83 % SI faces in the reverse-hybrid meshes. Fix that worked:
in-loop DLFL edge flips (delete_edge+insert_edge) + short-edge collapse
(collapse_edge_tri) + SI push every 25 steps: 6v 62 % → 3.4 %, 64v 83 % → 1.6 %.
Collapsing buried faces (3a/3b) and fold-loss ×10 (3c) did NOT work.

## 7. Open items
- DONE: 56.8k run → 0.9972 (+0.0015 over golden; my ≤+0.001 prediction was slightly pessimistic).
  219k run aborted (tangles below pixel scale). Supervision resolution (256²) caps usable faces at ~50-60k;
  raise GT renders to 512² before going higher. Also: `build_adj` had a hidden O(V²) dense GPU mask (44 GB at 109k V) — now skippable (`want_excl=False`).
- 6v `W_DEPTH=0` ablation; depth-fused hull field.
- Multi-shape benchmark (cow / bunny / Thingi10k) for the paper main table.

## 8. Ablation of the golden chain (2026-09-03)
Steps 2 (extrude carving) and 3 (stand-alone hull pull) are NOT needed: 1-4-5-6 gives 0.9959 / SI 1.5 % /
hair 0 / back 7.3° (golden v1: 0.9957 / 1.4 % / 8 / 7.9°). Step 4 IS needed: subdividing a tangled mesh
multiplies the tangles (1-5-6: 0.9948 but SI 13 %; skip-4 variants 26-33 % SI). The hull field lives inside
the phase-4 loop, which is what made 2 and 3 redundant. Rule: **clean (DLFL flips/collapses) before subdividing.**
IoU alone would have ranked 1-5-6 as "almost golden" — always report SI/folds/dihedral alongside.

## 9. Representation ablation: DLFL vs generic numpy ops (2026-09-03)
Same chain 1(mesh)→4→5→6 with every DLFL call replaced by array-based equivalents (`generic_ops.py`,
`GENERIC_OPS=1`; same rules/guards; Open3D has no per-edge flip/collapse primitives so numpy was used):

| | DLFL (ablation D) | generic |
|---|---|---|
| Step 4 final | 0.9868, SI 2.1 % | (see log) |
| Step 5 final | 0.9915, 36.9k f, SI 4.0 % | 0.9916, 33.0k f, SI 3.0 % |
| Taubin ×5 | **0.9959**, hair 0, back 7.3°, SI 1.5 % | **0.9957**, hair 3, back 6.9°, SI 0.9 % |

Conclusion: the score does NOT come from TopMod. Flip / collapse / subdivide are standard local ops; the
generic version stays watertight (check_watertight asserted every 25 steps) and lands within noise.
Only difference: the DLFL collapse is more conservative (84 vs 201 collapses on the same input), so the
generic chain ends with ~10 % fewer faces. What TopMod provides is manifold closure by construction and
an operator-program record, not accuracy. Paper claim must be phrased accordingly.

## 10. Positioning conclusions (2026-09-03, Boss discussion)
- **Score does not come from TopMod.** Generic numpy flip/collapse/subdivide with the same guards reproduce
  the chain (0.9957 vs 0.9959). TopMod's value = manifold closure by construction (generic version is
  manifold only because every guard was re-implemented by hand: empirical, not structural) + operator program.
- **Guards** = link condition (collapse), "new diagonal not already an edge" + non-degenerate + less-folded
  (flip); subdivision needs none. All textbook; any library can implement them.
- **What only DLFL offers here**: genus-changing manifold ops (`insert_edge` Case B / `add_handle`,
  `delete_edge`). Not used on armadillo (genus 0). Open3D/trimesh/pymeshlab have no such primitive;
  DMesh can change topology but only as a non-manifold soup.
- **DMesh's real advantages**: topology freedom, no initial topology, open/non-manifold surfaces, point-cloud
  recon, slightly better at equal low face count (0.9894 vs 0.9865). Its costs: soup output, resolution
  locked at 5–9k faces, CGAL WDT per step.
- **"No initialization needed" holds only with enough views**: DMesh 6v from random points collapses to
  0.5297; with our manifold mesh as init 0.9705; ours pure 6v 0.9457–0.9555. The closed-manifold
  constraint is a regularizer under sparse views. Trade-off: topology freedom (DMesh) vs manifold prior (ours).
- **Our family** = remeshing-during-inverse-rendering (Palfinger 2022 Continuous Remeshing; Nicolet 2021
  Large Steps). Extras over that line: hull distance field (space carving as loss), clean-before-subdivide,
  Taubin finish, DLFL op set incl. genus ops. Weakness shared with that line: fixed genus — rocker-arm PoC
  (genus 1) is the test of whether hull evidence + add_handle closes this gap.
- Paper baselines to run: DMesh (done), Continuous Remeshing (Palfinger), FlexiCubes/DMTet (grid-based).

## 11. Where we stand (2026-09-04, Boss review)
Self-intersection "measurable and controllable" is NOT a method advantage: DMesh is intersection-free by
construction (WDT faces); Palfinger could add the same Open3D check in a day. It is an engineering property
we report honestly (0-1.4 %), nothing more.

| | DMesh | Palfinger (continuous remeshing) | generic ops (Open3D/numpy) | **ours** |
|---|---|---|---|---|
| manifold output | no (soup) | yes, by construction of the op set | only if every guard is hand-written | **yes, by construction (DLFL)** |
| self-intersections | **zero by construction** | not measured | not measured | measured, 0-1.4 %, not zero |
| genus change from a sphere | yes (probabilistic) | no | no primitive | **yes: image evidence + add_handle (rocker-arm/3holes/fertility)** |
| accuracy, 64 views, armadillo ho16 | 0.9894 | (not run yet) | 0.9957 | **0.9972** |
| resolution | locked 5-9k faces | free | free | free (coarse-to-fine) |
| sparse views (6v) | collapses (0.53) | ? | = ours | 0.95 (manifold prior) |
| operator program (replayable / editable) | no | no | no | **yes** |
| speed (armadillo 64v) | **6-10 min** | ? | ~20 min | 25-35 min |

Real, defensible contributions: (1) manifold-guaranteed genus change driven by image evidence;
(2) accuracy via coarse-to-fine on a manifold representation (DMesh cannot subdivide);
(3) manifold prior helps under sparse views. Weak points to state: speed, SI not zero, 4th fertility
tunnel below 256^2 resolution, Palfinger baseline still to be run.

## 12. Early-hole experiment + collapse-runaway bug (2026-09-04)
Boss hypothesis: "CC subdivides so many times mainly because of the hole — if we added the hole one
step earlier (on the coarse mesh) we may not need so many CC passes." **Confirmed, once the chain is
fair.** Fair early-hole chain = cc3 -> clean 400 -> add_handle -> **RESUME cc4 + despike (rest of Stage 1)**
-> Stage 4 -> Stage 5 -> AUTO Taubin. The hole is opened on the ~1.8k-face coarse mesh, then the same
cc4 subdivision the normal chain uses is applied afterwards.

| rocker-arm | normal chain (subdivide, then hole) | early-hole v3 (hole at cc3, then cc4) |
|---|---|---|
| faces when hole opened | 5.7k | **1.8k** |
| hole opens clean | 400 steps, hair 495 | 400 steps, hair **495, SI 0 %** |
| final V | 14,501 | 16,743 |
| genus / SI | 1 / 0 % | 1 / **0 %** |
| ho16 | 0.9975 | **0.9979** |

Opening the handle on the coarse mesh is cheaper AND cleaner (SI stays 0 while the ring is small), then
cc4 restores resolution. Earlier v1/v2 scored low (0.994) only because those chains skipped cc4+despike
entirely — that was a chain-construction error, not evidence against the hypothesis.

**Collapse-runaway bug (fixed).** Collapse threshold was "shorter than 50 % of the *current* mean edge".
Each collapse raises the mean edge, so a fresh batch of edges qualifies next pass -> positive feedback
eats the mesh (fertility cc3 1.9k->378 faces genus 8; 3holes final lost 24 % of vertices). Two-layer fix:
(a) `COLLAPSE_FRAC=0.02` caps collapses per pass at 2 % of faces (slows it, not a cure);
(b) `COLLAPSE_ABS`: threshold = `COLLAPSE_RATIO x me0`, where `me0` is the mean edge length computed
**once before the loop**. With the absolute threshold, 3holes finals recovered to genus 3 / ho16 0.9966
with vertex count stable at 10,127 (no more shrinking).

Artifacts: `results_genus/rockerarm_early_auto.npz` (early-hole v3, genus 1, 16,743 V, ho16 0.9979),
`results_genus/threeholes_abs_auto.npz` (genus 3, ho16 0.9966, COLLAPSE_ABS).

### 12b. Fertility early-hole (2026-09-04)
| fertility | normal chain | early-hole v3 |
|---|---|---|
| faces when hole opened | 3.9k | 1.7k |
| handles added | 3 | 5 |
| genus (GT = 4) | 3 (under by 1) | 5 (over by 1) |
| final V | 7,356 | 8,983 |
| SI | 5 % | **1.0 %** |
| ho16 | 0.9885 | **0.9885** |

Early-hole keeps the same accuracy and drops SI 5 %->1 %, but tunnel detection on the 1.7k-face
coarse mesh is more sensitive: it splits one tunnel into two (or catches an extra 11-15 px shallow
mouth), giving genus 5 vs GT 4 — the normal chain under-counts (3), the early chain over-counts (5).
Neither hits 4 exactly. Net: opening the hole on the coarse mesh is cleaner (lower SI) across all three
shapes and equal-or-better accuracy; the cost is more sensitive tunnel counting that needs a sturdier
dedup on the coarse mesh. Artifact: `results_genus/fertility_early_auto.npz`.

## 13. Curvature-adaptive remeshing on DLFL ops (2026-09-04, Boss review of fertility density)
Boss: "why does the head need more vertices but get fewer, and the legs the opposite?" Measured:
GT allocates by detail (density 226 at the base -> 413 at the head); ours was inverted (base 4.4x GT,
head 1.1x). Mechanism: verts migrate into flat/concave regions during the sphere->shape deformation;
uniform cc / SUBDIV_ALL lock the imbalance in; collapse is absolute-threshold; nothing in the chain
allocates by curvature.

`ADAPT_REMESH` (phase4, every COLLAPSE_EVERY steps): per-vertex target edge length
L(v) = me0 * lerp(TMAX=1.6 -> TMIN, smoothstep(dihedral, 8..30 deg)), split faces whose longest edge
> 4/3 L (DLFL subdivide_edge), collapse edges < 4/5 L (DLFL collapse, per-vertex threshold).
Iteration history — every step was a Boss catch:
- v1: Stage-4 adaptive + Stage-5 SUBDIV_ALL -> 138k faces, pure-Python DLFL passes >3 min, timeout.
  Stage-5 is now adaptive-only (SUBDIV_ALL redundant with adaptive growth). ADAPT_MAX_F=60000.
- v2: fertility ho16 0.9960 but SI 39.5 %: **self-intersection jitter reads as curvature** (30-70 deg
  band) -> refined -> more jitter (SI 3.9 % -> 46 %). Fix = "clean before subdividing" in continuous
  form: curvature measured on a Taubin-smoothed copy (ADAPT_SMOOTH_K=3), SI faces + 1-ring never
  split, global gate ADAPT_SI_GATE.
- v3: gate 3 % never opened on fertility (SI floor via collapse/flip/push is ~3.2 %) -> 1,010 V, 0.9545.
  Gate 5 %; the local SI-ring exclusion is the real mechanism.
- v4: 2 px floor on L (ADAPT_LMIN_PX, px = 2*max_radius/256 from the camera setup). fertility fixed:
  **ho16 0.9962 (old chain 0.9885), SI 0.8 %, 12.2k V, density/GT 2.2-3.4x across bands (inversion
  gone)**. rocker-arm 0.9984 but 31.7k V (3.2x GT), mean edge 1.6 px, cap-limited.
- v5: Botsch-Kobbelt 4/3 / 4/5 thresholds. End state unchanged (rocker-arm 31.3k V, 1.6 px, 0.9985;
  fertility 12.3k V, 0.9962): the B-K equilibrium mean is ~0.8 L, so L = 2 px *is* a 1.6 px mesh.
  The floor works; 2 px was the wrong number for "earned" resolution (GT-decimation: 36.5k faces ->
  0.9985; GT rocker-arm itself is 20k faces at 2.8 px).

Open (Boss, 2026-09-04): points went to the LEGS, not the body. Dihedral measures 1/radius, not
detail or fit error: a thin limb is "high curvature" even when already well fitted. Space carving
exists only as a position loss (voting hull, W_T=20) and plays no part in where to refine.
Proposed next: error-driven refinement — split score = per-face silhouette/depth residual over the
64 views (nvdiffrast triangle-id buffer) + hull violation (HF.dist), curvature secondary.

| fertility | old chain | ADAPT v5 |
|---|---|---|
| ho16 / SI | 0.9885 / 1 % | **0.9962 / 0.5 %** |
| V (GT 4.5k) | 7.4k | 12.3k |
| density/GT base..head | 4.4 .. 1.1x (inverted) | 3.1 .. 2.4x (flat) |
| rocker-arm ho16 / SI / V (GT 10k) | 0.9979 / 0 % / 16.7k | 0.9985 / 0.2 % / 31.3k |

## 14. Residual-driven refinement (2026-09-05, Boss: "points should grow on the body, not the legs")
Dihedral "curvature" = 1/radius: thin limbs are refined even when already fitted. Replaced the
drive with the image-fit error itself (`ADAPT_MODE=residual`): per-face error over the 64 views
(|sil - GT| + W_DEPTH |depth - GT| on covered pixels via the nvdiffrast triangle-id buffer, plus
missing GT-foreground pixels attributed to the nearest rendered face), divided by covered
pixel-views -> absolute, pixel-scale, shape-agnostic. Space carving stays a position loss only.
- v7 (quantile-normalised): placement right (fertility splits 60-70 % mid-body, base 5-20 %) but
  by construction half the mesh is "fitted" -> coarsened: fertility 2.4k V / 6 px / 0.9899.
- v8 (absolute thresholds + KEEP policy: > 0.05/px unfit -> halve local edge; < 0.01/px and flat ->
  may coarsen; else keep current local edge; collapse threshold separated so keep-verts only lose
  slivers): fertility **0.9974**, SI 0.5 %, hair 0, but 18.8k V (4.2x GT) -- the inherited base
  over-density is never coarsened (base residual is not < 0.01).
- v8b (coarsen wherever fitted, R_KEEP 0.02, KEEP_FLAT=0): fertility **0.9969 / SI 0.5 % / 9.4k V
  (2.1x GT) / 2.9 px**; density/GT 2.3 2.4 2.2 1.8 1.7 1.9 (flat, inversion gone).
- Split/collapse caps: uncapped pure-Python DLFL ran 25 min without finishing a pass; 2 %/2 % is
  asymmetric (a split adds ~4 verts, a collapse removes 1) -> linear growth. 5 % / 20 %.

| fertility (GT 4.5k V) | ho16 | SI | V | edge px | dens/GT base..head |
|---|---|---|---|---|---|
| old chain | 0.9885 | 1 % | 9.0k | 2.7 | 4.1 .. 1.6 (inverted) |
| v5 curvature | 0.9962 | 0.5 % | 12.3k | 2.6 | 3.1 .. 2.7 |
| v8 residual+keep | **0.9974** | 0.5 % | 18.8k | 2.1 | 4.9 .. 3.5 |
| v8b residual+coarsen | 0.9969 | 0.5 % | **9.4k** | 2.9 | 2.3 .. 1.9 |

| rocker-arm (GT 10.0k V) | ho16 | SI | V | edge px |
|---|---|---|---|---|
| old chain | 0.9979 | 0 % | 16.7k | 2.2 |
| v5 curvature | 0.9985 | 0.2 % | 31.3k | 1.6 |
| v7 residual | 0.9982 | 0 % | **15.9k** | 2.4 |
| v8 residual+keep | 0.9985 | 0 % | 25.2k | 1.9 |

Where splits go (fertility v8, Stage-5, bands base->head): 13 / 22 / 24 / 21 / 12 / 8 %. The head
stays relatively least dense in every variant: at 256^2 its features are sub-pixel, the loss cannot
see them, so no residual-driven rule will refine there. Supervision resolution (512^2) is the lever.
Cost: pure-Python DLFL flip_sweep dominates (~4 min / 100 steps at 25k V); collapse 2-6 s / pass.

## 15. How others decide where to refine (survey, 2026-09-05) + head-to-head on our loop
| family | signal that sets the local target edge length | needs | source |
|---|---|---|---|
| curvature sizing field | L = sqrt(6 eps/k - 3 eps^2), k = max principal curvature (cot-Laplace H + angle-deficit K), gradation-limited, split > 4/3 L, collapse < 4/5 L; one parameter eps | geometry only | Dunyach 2013 (EG short), Botsch-Kobbelt 2004; CGAL / PMP implementations |
| velocity closed loop | ref_len *= 1 + (nu - nu_ref) gain, nu = per-vertex Adam speed; every iteration; coarser where still moving, finer once settled | optimizer state | Palfinger 2022, Continuous Remeshing (code: core/opt.py) |
| target-normal face score | FS(f) = sum over super-samples projected onto the target of (1 - n_vertex . n_face) | a target surface (SDF / points) -- not available in inverse rendering | ROAR 2023 |
| curvature + uniform, periodic | every 130-200 it: split high-(k1,k2) edges, uniform split elsewhere by mean edge, collapse below min, flips to valence 6, tangential smoothing; genus FIXED from GT via Gauss-Bonnet | GT genus | "Inverse Rendering for High-Genus Surface Meshes", 3DV 2026 (baseline Nicolet; Armadillo IoU 0.928, genus-4 0.885, genus-5 0.750) |
| image importance + flow | attention map modulates split thresholds; "attention flow" propagates importance to neighbours (without it, low-attention regions stay under-refined) | images | SIGGRAPH 2024 poster on Continuous Remeshing |
| image residual (ours) | mean image error per covered pixel-view per face -> halve / keep / coarsen | images | this work (section 14) |
MVS tradition (Vu 2012, facetwise 2020) refines vertices along the photometric gradient with
resolution tied to image pixels; no one in inverse rendering sizes the mesh by the loss residual.
Take-aways: (1) our residual sizing is defensible and closest to the attention-flow poster, whose
lesson is gradation (ADAPT_GRADE); (2) if curvature is kept, use Dunyach with eps in pixels -- one
parameter, tied to supervision; (3) Palfinger's "do not refine what is still moving" is a principled
tangle guard (jitter = motion); (4) the under-refined head is shared by every image-driven method;
(5) 3DV-2026 needs the GT genus, we discover it -- a clear differentiator.
Head-to-head (same DLFL loop, guards, 3 px floor, gradation 1.0, caps 5/20 %; fertility + rocker-arm,
early64v -> Stage-4 -> Stage-5 -> AUTO Taubin): modes curvature | dunyach | velocity | curv_uniform |
residual. Results: see table below (task mode_matrix).

### 15a. Results — fertility (5 criteria, equal settings; task mode_matrix, 2026-09-05)
Equal settings for all: DLFL loop, SI-ring exclusion + 5 % gate, 3 px floor, gradation alpha 1.0,
split/collapse caps 5 % / 20 %, TMIN 0.35 / TMAX 1.6 x me0, coarsen-wherever-fitted from Stage 4,
early64v -> Stage-4 1200 -> Stage-5 1200 -> AUTO Taubin. Files `results_genus/fertility_mx_*_auto.npz`.

Metrics: ho16 = silhouette IoU on 16 held-out views (our exam); VolIoU / CD = volume IoU on a
256^3 occupancy grid and symmetric Chamfer after ICP (3DV-2026 metric family, `eval_cd_iou.py`).

| criterion | ho16 sil-IoU | **VolIoU** | CD | V (GT 4.5k) | edge px | SI | hair | density/GT base..head |
|---|---|---|---|---|---|---|---|---|
| curvature (dihedral proxy, ours v5) | 0.9822 | 0.9090 | 0.01290 | 1.2k (0.26x) | 8.5 | 2.3 % | 118 | 0.38 0.32 0.20 0.20 0.26 0.24 |
| dunyach (principal curvature, eps 0.5 px) | 0.9847 | 0.9302 | 0.01205 | 1.9k (0.41x) | 6.6 | 1.0 % | 24 | 0.52 0.48 0.32 0.38 0.38 0.35 |
| velocity (Palfinger closed loop) | 0.9706 | 0.9379 | 0.01149 | 2.8k (0.63x) | 5.0 | **0.4 %** | 227 | 1.16 1.05 0.61 0.45 0.42 0.38 |
| curv_uniform (3DV-2026) | 0.9937 | 0.9736 | 0.00866 | 3.2k (0.72x) | 5.2 | 0.8 % | 0 | 0.90 0.83 0.63 0.62 0.62 0.65 |
| **residual (ours)** | **0.9942** | **0.9761** | **0.00811** | 4.2k (0.93x) | 4.5 | 1.4 % | 0 | **1.04 0.98 0.90 0.85 0.76 0.90** |
| (ref) tuned v8b residual chain | 0.9969 | 0.9861 | 0.00708 | 9.4k (2.1x) | | 0.5 % | | |

Note the VolIoU spread (0.909 -> 0.976) is 10x wider than the silhouette spread (0.982 -> 0.994):
the volume metric punishes the coarsened, unfitted regions much harder than the 16-view exam does.
Same ranking under both metrics.

Conclusions:
1. Pure curvature criteria (dihedral AND proper principal curvature) coarsen fertility to 1/4-2/5 of
   GT: "geometrically flat" is read as "needs no vertices", but flat is not fitted. Silhouette accuracy
   is worst. A better curvature estimator (Dunyach) does not fix this -- the signal is wrong, not
   its estimate.
2. Palfinger's velocity loop gives the lowest self-intersection (0.4 %) but the worst accuracy:
   it coarsens where vertices still move, and on fertility the worst-fitted regions are exactly the
   ones still moving. Good tangle guard, wrong sizing signal for accuracy.
3. 3DV-2026's curvature+uniform is rescued by its *uniform* half (0.9937): not-by-curvature beats
   by-curvature here.
4. Residual sizing is the most accurate and gives the flattest density/GT profile (1.04 -> 0.90),
   i.e. resolution follows GT's allocation; the head is relatively best served of all criteria.
5. Caveat: the shared aggressive coarsening settings put every criterion below GT vertex count and
   below our tuned v8b (0.9969 / 9.4k V). The matrix compares *criteria* at one operating point,
   not each method's best. rocker-arm half: task mode_matrix_rock (pending).

### 15b. Results — rocker-arm (same 5 criteria, same settings; task rock_matrix3, 2026-09-05)
Same knobs as 15a. GT genus 1, GT V 10.0k. Files `results_genus/rockerarm_mx_*_auto.npz`,
visual `results_genus/rockerarm_mx_compare.png` (front/back, GT + 5 criteria).

| criterion | p4 ho16 | p5 ho16 | **Taubin ho16 sil-IoU** | **VolIoU** | CD | V (GT 10.0k) | edge px | SI | density/GT base..top | wall (5 par.) |
|---|---|---|---|---|---|---|---|---|---|---|
| curvature (dihedral) | 0.9958 | 0.9969 | 0.9980 | 0.9936 | 0.00584 | 11.0k (1.09x) | 2.9 | 0 % | 0.87 1.01 1.15 1.20 1.19 1.04 | 83 min |
| dunyach | 0.9949 | 0.9953 | 0.9970 | 0.9887 | 0.00600 | 4.7k (0.47x) | 4.3 | 0 % | 0.40 0.41 0.47 0.55 0.48 0.47 | 79 min |
| velocity (Palfinger) | 0.9947 | 0.9953 | 0.9970 | 0.9895 | 0.00598 | 5.3k (0.53x) | 4.0 | 0 % | 0.41 0.46 0.57 0.61 0.55 0.52 | 80 min |
| curv_uniform (3DV-2026) | **0.9969** | **0.9972** | **0.9985** | **0.9937** | 0.00586 | 25.4k (2.53x) | 1.9 | 0 % | 1.70 2.27 2.89 2.85 2.79 2.39 | 100 min |
| residual (ours) | 0.9955 | 0.9969 | 0.9984 | 0.9936 | **0.00584** | 17.7k (1.76x) | 2.2 | 0 % | 1.41 1.59 1.85 2.00 1.81 1.77 | 88 min |

VolIoU confirms the silhouette ranking: curvature / curv_uniform / residual tie at 0.9936-0.9937
(curvature with 11k V is the cheapest of the three here), dunyach / velocity 0.989.

Reference: tuned v8 residual chain 0.9985 / 25.2k V; early_auto (no adaptive) 0.9979.

Conclusions (rocker-arm):
1. Spread is tiny: 0.9970-0.9985 after Taubin, every criterion SI 0 %, genus 1, all five visually
   indistinguishable from GT in the compare image. Rocker-arm is thin-walled with a wide tunnel, so
   the silhouettes constrain almost every surface point -- there is no "flat but unfitted" region for
   a curvature criterion to starve. The criterion question is a *fertility* question, not a
   rocker-arm question.
2. The remaining spread is vertex budget, not the sizing signal: curv_uniform 0.9985 at 25.4k V,
   residual 0.9984 at 17.7k V (-30 % V for -0.0001), curvature 0.9980 at 11.0k V. ho16 vs V is a
   smooth curve here; residual sits on the best point of it, curv_uniform buys the last 1e-4 with
   uniform splitting everywhere (density 2.3-2.9x GT on the body).
3. Density profiles are flat for every criterion (no inversion): on this shape the shape itself is
   uniformly "hard", so any criterion allocates roughly uniformly. Dunyach/velocity coarsen to ~0.5x
   GT uniformly and lose 0.0015 -- the same "flat = done" failure as fertility, just milder.
4. Combined verdict over both shapes: residual is the only criterion that is top-2 on both
   (fertility 1st by 0.0005 with the flattest allocation; rocker-arm 2nd by 0.0001 with 30 % fewer
   vertices). curv_uniform is the strongest baseline and would be the honest comparison line in the
   paper; pure curvature criteria (dihedral, Dunyach) and Palfinger velocity sizing are dominated.
5. Ops: wall 79-100 min per chain at 5-parallel *with* a ComfyUI server on the Windows host holding
   ~15 GB (found via `nvidia-smi.exe`, invisible to WSL nvidia-smi); after it was shut down the
   chains ran ~3x faster. Always check the host side when the GPU is full.

### 15c. Palfinger velocity guard on top of residual sizing (ADAPT_VEL_GUARD=2; task rock_matrix3)
Faces whose vertices moved > 2x the median displacement since the last remesh are not split
(borrowed from Palfinger's "don't refine what is still moving"); sizing stays residual.
Files `results_genus/{fertility,rockerarm}_mx_residual_vg_auto.npz`.

| shape | variant | sil-IoU | VolIoU | CD | V | SI | wall |
|---|---|---|---|---|---|---|---|
| fertility | residual (15a) | 0.9942 | 0.9761 | 0.00811 | 4.2k | 1.4 % | |
| fertility | residual + vel guard | **0.9962** | **0.9827** | **0.00741** | 7.4k | **0.7 %** | 23 min |
| rocker-arm | residual (15b) | 0.9984 | 0.9936 | 0.00584 | 17.7k | 0 % | 88 min (5 par.) |
| rocker-arm | residual + vel guard | 0.9982 | 0.9934 | 0.00586 | 16.4k | 0 % | 31 min (2 par.) |

Verdict: on fertility the guard halves SI (1.4 -> 0.7 %) and improves every metric, but V also
goes 4.2k -> 7.4k, so part of the gain is budget: not splitting moving faces keeps the split cap
for stable faces, which then survive collapse. On rocker-arm it is neutral (-0.0002, -7 % V).
Keep it on (cheap, never hurts, SI down); it is a tangle guard, not a sizing signal -- consistent
with 15a conclusion 2. Still below tuned v8b (0.9969 / VolIoU 0.9861) because of the aggressive
shared coarsening settings of the matrix.

## 16. External baseline: Palfinger 2022 (original code) on our setup (2026-09-05)
Paper: W. Palfinger, "Continuous remeshing for inverse rendering", Computer Animation and Virtual
Worlds 33(5), e2101, 2022 (CGI 2022 special issue; Profactor GmbH, Austria). Code:
github.com/Profactor/continuous-remeshing (Resource/continuous-remeshing; torch_scatter shim added
because no wheel exists for torch 2.12+cu130). Runner: `despike/run_palfinger.py`.
Setup: its own optimiser + remesher, defaults (lr 0.3, nu_ref 0.3, edge_len_lims (0.01, 0.15),
tol 0.5, gain 0.2, remesh every step), level-2 icosphere init, 1200 steps, OUR 64 star cameras
(8 az x 8 el, distance 2R, fov 2 atan 0.5) at 256^2, OUR GT mesh as target, OUR ho16 exam on the
output. Optimised in its unit-sphere frame, rescaled. Its supervision is its own: normal images +
alpha. Near-plane fix: its default projection (near = 1.0) clips the front of an object of radius 1
at distance 2 -> ho16 0.28; with near 0.1 and the same fov the run is sane.

| shape | Palfinger ho16 | genus (GT) | V | hair | SI | plateau | ours ho16 / genus / V |
|---|---|---|---|---|---|---|---|
| fertility | 0.9398 | 0 (4) | 55.6k (12x GT) | 24.8k | 1.9 % | from step 200 | **0.9969** / 5 / 9.4k (v8b) |
| rocker-arm | 0.9674 | 0 (1) | 56k | 17.3k | 0.4 % | from step 200 | **0.9985** / 1 / 25k (v8) |
| armadillo | **0.9965** | 0 (0) | 37.9k | 1 | 0.0 % | 216 s wall | 0.9972 / 0 / 57k |
| kitten (3DV-2026 shape) | 0.9991 | 0 (1) | 67.2k | 0 | 0.3 % | 479 s wall | ours v8 full chain: sil-IoU **0.9994**, VolIoU **0.9982**, CD 0.00666, **genus 1 discovered** (1 handle after cc3+Stage-4, second detect round found no tunnel evidence), 19.4k V, SI 0 %; file `results_genus/kitten_v8_auto.npz`, visual `results_genus/kitten_compare.png` |

Visual: `results_genus/fertility_vs_palfinger.png` (GT | ours v8b | Palfinger): every tunnel is a
stretched membrane in Palfinger's result -- fixed-genus deformation cannot open a hole, the
silhouette IoU is capped and the run plateaus after 200 steps. Vertex count runs to its lower edge
length limit (0.01 in the unit sphere ~ 1.2 px) -- the whole mesh is refined to the floor, no
notion of supervision resolution. Self-intersection is genuinely low (0.4-1.9 %), consistent with
the velocity-mode observation in section 15a.
Armadillo (genus 0, no topology factor): Palfinger 0.9965 vs our 0.9972 with FEWER vertices
(37.9k vs 56.8k), SI 0 %, 216 s. On pure genus-0 accuracy the two are within 0.0007; our margin is
topology (genus discovery) and vertex economy at equal accuracy is NOT ours here. Honest headline.
Kitten: Palfinger scores ho16 0.9991 / Volume IoU 0.9968 / CD 0.00678 with genus 0 on a genus-1
shape -- the tunnel (between tail and body) is thin in every silhouette, so the missing hole barely
costs IoU. Topology matters for the metric only when the tunnel is wide (fertility, rocker-arm).
Ops note (2026-09-05): two batches died at launch because of an unquoted `$X` inside a bash
env-prefix list (empty expansion ends the assignment region -> `BASE_NPZ=...` executed as a
command, rc 127, no traceback). Use `env $X VAR=... cmd`. Also `STOP_AFTER=cc3` saves
`cow_{TAG}.npz`, so pass TAG explicitly.
Fairness caveats: different supervision (normals+alpha vs our sil+depth+diffuse+hull); 1200 steps is
far past its plateau; its defaults were not tuned for our shapes. The armadillo row (genus 0, no
topology factor) is the clean accuracy-only comparison.

### 16b. Same metric family as 3DV-2026 / ICASSP-2026 (Chamfer + Volume IoU after ICP), our computation
`despike/eval_cd_iou.py`: ICP point-to-plane (50k samples), CD = mean of both NN directions (100k
samples each, our normalised frame; bbox-normalised value also given), Volume IoU on a 256^3
occupancy grid over the GT bbox. The papers do not publish their sample counts / voxel resolution.

| shape | mesh | V | CD | CD / bbox | Volume IoU | ho16 (ours) |
|---|---|---|---|---|---|---|
| armadillo (g0) | ours golden | 28.4k | 0.00671 | 0.00142 | 0.9885 | 0.9972 |
| armadillo (g0) | Palfinger 2022 | 37.9k | **0.00627** | 0.00133 | **0.9938** | 0.9965 |
| fertility (g4) | ours v8b (genus 5) | 9.4k | **0.00711** | 0.00167 | **0.9862** | 0.9969 |
| fertility (g4) | Palfinger (genus 0) | 55.6k | 0.01226 | 0.00287 | 0.9599 | 0.9398 |
| rocker-arm (g1) | ours v8 (genus 1) | 25.2k | **0.00584** | 0.00150 | **0.9937** | 0.9985 |
| rocker-arm (g1) | Palfinger (genus 0) | 56.6k | 0.01330 | 0.00342 | 0.9382 | 0.9674 |

3DV-2026 (arXiv 2511.18680) reports, on ITS setup (36 views @ 1024^2 RGBA, custom CUDA renderer,
genus given from GT, baseline Nicolet 2021): Armadillo Volume IoU 0.928 (Nicolet 0.897), Kitten g1
0.713, Amphora g2 0.792, Pretzel g3 0.864, Birthday g4 0.885, Sorter g5 0.750. ICASSP-2026
(2601.12155, persistent-homology priors for camera placement, topology still fixed): Kitten 0.919,
Birthday 0.771, Sorter 0.746. No code released for either; shapes from Stanford / GSO / Thingi10K /
Gu / Crane.
Reading: (1) on the volume metric Palfinger beats our golden on genus-0 armadillo (0.9938 vs
0.9885) even though our silhouette ho16 is higher -- our surface is slightly off in depth where the
silhouette cannot see; (2) on genus>0 shapes we win by 0.03-0.06 Volume IoU because their
membranes fill the tunnels; (3) the 3DV numbers (0.71-0.93) are far below both of ours, but they
are on different objects and a different protocol -- only a run on their shapes with their
protocol is a comparison. Comparable in principle: same metric family, and they need the GT
genus while we discover it.

### 16c. Kitten (3DV-2026 genus-1 test shape) -- three-way, our exam + 3DV metric family (2026-09-05)
| method | genus | sil-IoU ho16 | VolIoU | CD | V |
|---|---|---|---|---|---|
| 3DV-2026 (published, genus given, 36 views @1024^2) | 1 (given) | -- | 0.713 (their IoU) | -- | -- |
| ICASSP-2026 follow-up (published) | 1 (given) | -- | 0.919 | -- | -- |
| Palfinger 2022 original code, our 64 views @256^2 | 0 (fixed) | 0.9991 | 0.9968 | 0.00678 | 67.2k |
| **ours v8 chain, our 64 views @256^2** | **1 (discovered)** | **0.9994** | **0.9982** | **0.00666** | 19.4k |

Reading: (a) we recover the correct topology from images alone and beat Palfinger on all three
metrics with 3.5x fewer vertices; (b) the published 3DV/ICASSP numbers are not directly comparable
(their own renders/cameras, their IoU definition, no code) -- our absolute values are far higher
mostly because 64 star views + depth + our exam make Kitten an easy shape; (c) as noted in 16, the
tunnel is thin, so genus 0 costs Palfinger only 0.0014 VolIoU here -- Kitten is a weak witness for
"topology matters"; fertility / rocker-arm (wide tunnels) are the strong ones.

## 17. DMesh on the genus shapes + normal-map supervision A/B (2026-09-05, Boss questions)

### 17a. DMesh (official code, 64v hires config = the armadillo one) on kitten / fertility / rocker-arm
Task dmesh_genus3. DMesh output is a non-manifold soup (genus undefined). Files `results_genus/*_dmesh_last.obj`.

| shape | DMesh ho16 | DMesh VolIoU | CD | V | ours v8 ho16 / VolIoU | Palfinger ho16 / VolIoU | wall |
|---|---|---|---|---|---|---|---|
| kitten | 0.9944 | 0.9895 | 0.00738 | 3.9k | 0.9994 / 0.9982 | 0.9991 / 0.9968 | 20 min |
| fertility | 0.9886 | 0.9788 | 0.00707 | 3.1k | 0.9974 / 0.9861 (v8b) | 0.9398 / 0.9599 | 15 min |
| rocker-arm | 0.9928 | 0.9803 | 0.00638 | 3.4k | 0.9985 / 0.9937 | 0.9674 / 0.9382 | ~12 min |

DMesh is last or second-last on every shape; as on armadillo it sits at its representation-locked
face-count ceiling (3-4k V). It does handle genus>0 without exploding (soup can be anything), so on
fertility/rocker-arm it beats Palfinger's genus-0 surface, but not ours. Paper table is now complete:
DMesh (topology-free, non-manifold), Palfinger (manifold, genus fixed), 3DV-2026 (genus given), ours
(manifold, genus discovered).

### 17b. Normal-map supervision (W_NORMAL=1, camera-space normal image L1, Palfinger-style)
Motivation (Boss): kitten eye sockets / inner ears looked worse than Palfinger; our shading term is a
1-channel |n.z| headlight (sign-blind, no lateral component), Palfinger supervises 3-channel normals.
Implementation: `run_64v.render_normals` + `make_gt_normals`, `phase4_inloop` knob `W_NORMAL`
(commit 25f489b). Task normal_ab: v8 chain + vel guard, kitten and fertility, W_NORMAL=1.

| shape | variant | ho16 | VolIoU | CD | V |
|---|---|---|---|---|---|
| kitten | v8 (no normal) | 0.9994 | 0.9982 | 0.00666 | 19.4k |
| kitten | v8 + normal | 0.9987 | 0.9955 | 0.00681 | 7.8k |
| fertility | v8 vel-guard (15c) | 0.9962 | 0.9827 | 0.00741 | 7.4k |
| fertility | v8 + normal | 0.9940 | 0.9703 | 0.00859 | 4.2k |

Head-band check (top 38 % of the long axis = head + ears; GT->pred sample distance, px @256):

| method | head p50 / p90 / p99 | body p90 |
|---|---|---|
| Palfinger | 0.26 / 0.46 / 0.65 | 0.46 |
| DMesh | 0.33 / 0.56 / 0.87 | 0.62 |
| ours v8 | 0.25 / 0.46 / 0.64 | 0.46 |
| ours v8 + normal | 0.28 / 0.48 / 0.67 | 0.48 |

Conclusions:
1. Negative result at this weight: the normal term LOWERS every metric on both shapes and halves the
   vertex count. Mechanism: the L1 on normals is a strong smoothness prior (it pulls toward the GT's
   smooth normals everywhere, and its residual is small once the surface is roughly right), so the
   residual-driven remesher sees "fitted" and coarsens; the silhouette/depth fit then loses. The
   render (`results_genus/kitten_head_closeup.png`) shows the + normal mesh more faceted, not less.
2. The "worse eyes/ears" impression does not survive measurement: ours v8 and Palfinger are identical
   in the head band (p90 0.46 px both, sub-pixel). What reads as detail in Palfinger's render is its
   67k-vertex high-frequency grain; the eye socket is a sub-pixel feature at 256^2 in both.
3. The lever for concavities is therefore resolution (512^2 supervision), not the supervision type.
   Not pursuing W_NORMAL further unless combined with a normal-aware split criterion (the remesher
   would have to count normal residual as unfit, which is the opposite of what it does now).

### 17c. Normal-map supervision, corrected (task normal_ab2, 2026-09-05)
Fixes vs 17b (commit a16db66): (1) normal residual now feeds `face_residual`, i.e. the split
criterion counts normal error as unfit; (2) `nrm=` logged per step; probe at W=1 showed the normal
term is 3.2x the silhouette term (kitten: sil 0.0043 vs nrm 0.0137), so W in {0.3, 0.1}.
Same v8 + vel-guard chain. 4 chains parallel with ComfyUI on the host: 88-99 min each.

| shape | W_NORMAL | ho16 | VolIoU | CD | V | p4 nrm at 1200 |
|---|---|---|---|---|---|---|
| kitten | 0 (v8) | 0.9994 | 0.9982 | 0.00666 | 19.4k | -- |
| kitten | 0.1 | 0.9994 | 0.9983 | 0.00665 | 18.7k | 0.0039 |
| kitten | 0.3 | **0.9995** | **0.9985** | 0.00665 | 20.9k | 0.0034 |
| kitten | 1.0 (17b, no residual) | 0.9987 | 0.9955 | 0.00681 | 7.8k | -- |
| fertility | 0 (15c vel-guard) | **0.9962** | **0.9827** | 0.00741 | 7.4k | -- |
| fertility | 0.1 | 0.9953 | 0.9784 | 0.00811 | 5.8k | 0.0131 |
| fertility | 0.3 | 0.9940 | 0.9728 | 0.00863 | 4.3k | 0.0275 |
| fertility | 1.0 (17b) | 0.9940 | 0.9703 | 0.00859 | 4.2k | -- |

Conclusions:
1. With the weight fixed and the residual wired in, the 17b collapse is gone on kitten: W=0.3 is
   the best kitten number so far (+0.0003 VolIoU, +0.0001 ho16) at the same vertex budget. Gain is
   at the noise floor -- the eye sockets are 0.3 px deep, there is nothing for normals to add at 256^2.
2. On fertility it still hurts, monotonically in W (0.9827 -> 0.9784 -> 0.9728), and the normal
   residual stays 4-8x higher than on kitten (0.013-0.028 vs 0.004): fertility's GT normals are not
   reachable from a 1-2k-vertex Stage-4 mesh, so the term acts as a smoothness prior that starves
   the p4 mesh (W=0.3 p4: 1.1k V, SI 4.7 %, ho16 0.9675) and p5 never recovers the budget.
3. Verdict: normal supervision is not the lever for concavities in this setup. Keep `W_NORMAL`
   available (0 default); if used, only after Stage 4 (p5 onward) and with W <= 0.3. Resolution
   (512^2) remains the open lever.

## 16d. External baseline: Nicolet 2021 "Large Steps" (official largesteps pkg) on our setup (task nicolet4b, 2026-09-05)
Harness `despike/run_nicolet.py` (commit 67d9981). Their method = (I + lambda L) parameterization
(alpha 0.95) + AdamUniform (lr 1e-2, x0.8 after each remesh) + Botsch-Kobbelt remeshing (gpytoolbox
port; target edge = 0.5 x mean edge at steps 400 and 800), icosphere level-3 start, 1200 steps.
Supervision is OURS (64 star views @256^2, silhouette + masked depth x0.35 + headlight diffuse, L1),
not their SH-lit scene renders, so the comparison isolates optimizer + remeshing. Exam = ho16 + VolIoU/CD.
Ops: cholespy 1.0.0 (their Cholesky solver) segfaults under torch 2.12+cu130 -> their CG solver
(1e-6 exact); gpytoolbox returns non-contiguous arrays -> ascontiguousarray before nvdiffrast.

| shape | genus (GT) | Nicolet genus | ho16 sil-IoU | VolIoU | CD | V | SI | wall |
|---|---|---|---|---|---|---|---|---|
| kitten | 1 | 0 | 0.9768 | 0.9684 | 0.01104 | 8.7k | 1.3 % | 4.7 min |
| fertility | 4 | 0 | 0.8963 | 0.8262 | 0.02700 | 8.4k | 3.1 % | 4.8 min |
| rocker-arm | 1 | 0 | 0.9581 | 0.9249 | 0.01555 | 8.1k | 0.5 % | 12.9 min |
| armadillo | 0 | 0 | 0.9593 | 0.8985 | 0.02034 | 8.6k | 2.4 % | 15.4 min |

Sanity vs the published numbers: 3DV-2026 report Nicolet at Armadillo VolIoU 0.8968 -- we get 0.8985
on our (different) setup, so on a genus-0 shape their metric scale and ours agree for this baseline.
On Kitten they report Nicolet 0.63, we get 0.97: their 36 views miss the tunnel entirely (they say so:
"120-360 views at 256^2 in rare cases where the rendered views fail to capture essential topological
features"), our 64 star views see it. View count, not the metric, explains the gap there.

### Master table (VolIoU / Chamfer after ICP, our computation; genus 0 = method cannot open holes)
| shape | GT g | Nicolet 2021 | Palfinger 2022 | DMesh 2024 | 3DV-2026 (published) | ours |
|---|---|---|---|---|---|---|
| armadillo | 0 | 0.8985 / 0.0203 | **0.9938** / -- | 0.98+ (LESSONS 1, ho16 0.9894) | 0.928 | 0.9885 / -- |
| kitten | 1 | 0.9684 / 0.0110 | 0.9968 / 0.0068 (g0) | 0.9895 / 0.0074 | 0.713 | **0.9982 / 0.0067** (g1) |
| fertility | 4 | 0.8262 / 0.0270 | 0.9599 / -- (g0) | 0.9788 / 0.0071 | -- | **0.9861 / 0.0071** (g5: one spurious handle, GT g4) |
| rocker-arm | 1 | 0.9249 / 0.0156 | 0.9382 / -- (g0) | 0.9803 / 0.0064 | -- | **0.9937 / 0.0058** (g1) |

Reading: on the three genus>0 shapes the order is ours > DMesh > Palfinger > Nicolet on every metric;
DMesh beats the two fixed-topology methods because a soup can be anything, and loses to us on
accuracy (representation-locked ~3-4k V). On armadillo (genus 0) Palfinger's normal-image supervision
at 55k V still beats our VolIoU (0.9938 vs 0.9885) -- unchanged honest weakness from 16b.

## 18. 512^2 supervision (task res512, 2026-09-06) + ops: /tmp wiped by reboot
`TRAIN_RES` knob (commit 9a466da): GT images, training renders, PX_SIZE and train-IoU follow it; exam stays
256^2. v8 chain + vel guard, ADAPT_MAX_F 100k, kitten / fertility / rocker-arm in parallel.

| shape | 256^2 (best) ho16 / VolIoU / V | 512^2 ho16 / VolIoU / CD / V | wall 512 |
|---|---|---|---|
| kitten | 0.9994 / 0.9982 / 19.4k | 0.9993 / 0.9975 / 0.00668 / 22.9k | 37 min |
| fertility | 0.9962 / 0.9827 / 7.4k (vel-guard); v8b 0.9969 / 0.9861 / 9.4k | 0.9968 / 0.9851 / 0.00690 / 14.6k | 25 min |
| rocker-arm | 0.9985 / 0.9937 / 25.2k | 0.9985 / 0.9902 / 0.00595 / 32.2k | 48 min |

No gain anywhere; VolIoU slightly lower on kitten and rocker-arm with 1.2-1.7x more vertices. Together
with 17c (normals) this closes the "head / concavity" question: the residual error is sub-pixel at 256^2
and neither more pixels nor more channels move it. Lever list for this benchmark is exhausted; move to
real data (DTU / splat renders).

Ops (2026-09-06 17:15): WSL rebooted; `/tmp/liou_cow_viz` (every intermediate npz incl. the 512 meshes
and the early64v bases) was wiped. Numbers above are from the task log; the 512 meshes are NOT archived.
Fix: `/tmp/liou_cow_viz` is now a symlink to `experiments/opseq_v5/out_liou/` (persistent). After any
reboot re-create it: `ln -sfn .../opseq_v5/out_liou /tmp/liou_cow_viz`. Archive to results_genus/
immediately after every run. The early64v bases must be regenerated before the next chain run.

## 19. Input fairness: the voting hull field (tasks fair / fair_ctrl / fert_g5b, 2026-09-06/07)
Boss: "what extra inputs do we use that others don't?" The hull field is built from GT silhouettes rendered
at **512^2** (`build_vote_hull(hires=512)`) while everyone (incl. our loss) sees 256^2 images -> an input
advantage. Knob `HULL_HIRES` (commit c8e1058), also in `phase7_handle.py` (genus discovery uses the hull).
All bases were rebuilt after the reboot (early64v lost), so all three variants share ONE new base per
shape; the hull512 row is the control on that same base (not the old numbers).

| shape | base (rebuilt) | hull 512^2 (control) | hull 256^2 | no hull (W_T=0) |
|---|---|---|---|---|
| kitten | genus 1, early64v 0.9928 | 0.9994 / 0.9982 / 19.5k | 0.9994 / 0.9982 / 18.7k | 0.9995 / 0.9983 / 18.7k |
| rocker-arm | genus 1, early64v 0.9839 | 0.9983 / **0.9935** / 15.6k | 0.9977 / 0.9546 / 16.8k (SI 1.1 %) | 0.9976 / 0.9567 / 15.5k (SI 1.4 %) |
| fertility | genus **3** (see below) | 0.9516 / 0.8292 (invalid base) | 0.9572 / 0.8529 | 0.9540 / 0.8299 |
(ho16 / VolIoU / V)

Findings:
1. kitten: the hull is irrelevant (three variants identical). Silhouettes alone constrain a genus-1 shape
   with a thin tunnel.
2. rocker-arm: the hull is worth **+0.04 VolIoU** and the 256^2 hull is as useless as no hull. So our
   rocker-arm number DOES depend on a 512^2 GT-silhouette hull that competitors never got. Paper must
   either (a) give every method the same hull (Nicolet/DMesh: one loss term), (b) report our no-hull
   row (0.9567, still > Nicolet 0.9249 but < DMesh 0.9803 and only = Palfinger 0.9382+), or (c) rebuild
   the hull from the 256^2 images with a sub-pixel-aware vote. (c) is the honest engineering fix.
3. Genus discovery is NOT reproducible on fertility: the rebuilt chain found 3 handles, not 5. Two
   causes found (phase7_handle): OUT_VOX=4 refused a membrane 2-3 vox outside the hull, and after
   relaxing to 2 the `add_handle` broke watertightness because the membrane sheets were pinched
   (entry/exit faces 0.9 edges apart, 1-rings overlapping 10 faces). Fix commit 6752884: joint search for
   a disjoint-ring face pair on the two sheets -> 4th handle inserted (genus 4). 5th still missing at
   the time of writing (fert_g5b runs the ablation on the genus-4 base). This is the top robustness
   item before any paper claim about "genus discovered from images".
4. Ops: never compare across rebuilt bases; a base rebuild after the reboot cost a day. Bases are now
   archived (`results_genus/*_early64v_base.npz`).
5. **Correction (2026-09-07): fertility GT genus is 4, not 5** (V-E+F = -6 on `fertility.obj`; verified;
   kitten 1, rocker-arm 1, threeholes 3, armadillo 0 all as assumed). Every archived fertility result
   (early_auto, v4..v8b, mx_*, nrm*) is genus 5 = ONE SPURIOUS HANDLE that the old chain inserted and
   that we had been reporting as "genus 5 correct". The VolIoU cost of the extra tube was small
   (0.986), which is why nothing flagged it. The rebuilt base (genus 4) is the correct one, so the
   "5th tunnel not found" above is the detector being RIGHT. Two actions: (a) genus-4 fertility rows
   from fert_g5b replace the old ones as the headline numbers; (b) find where the old chain's 5th
   handle went (false-positive analysis of `find_tunnel_by_rays`) -- a false tunnel is worse for the
   paper than a missed one.

### 19b. fertility on the correct genus-4 base (task fert_g4s3, serial, SI gate 30 %, 2026-09-07)
The rebuilt genus-4 base starts at SI 25 % (tube inserted on a pinched sheet, then cc4); the 5 % split
gate then blocked every split for 1200 steps (15c-style starvation: 1.2k V, VolIoU 0.83-0.85). With
`ADAPT_SI_GATE=0.3` the chain behaves normally. Peak VRAM per stage: p4/p5 1.46 GB allocated
(2.3-2.4 GB reserved), Taubin 0.04 GB -- the earlier "3.3 GB per chain" was mostly CUDA context.

| variant | p4 ho16 / V / SI | p5 ho16 / V | Taubin ho16 | VolIoU | CD | wall |
|---|---|---|---|---|---|---|
| hull 512^2 | 0.9917 / 6.0k / 2.7 % | 0.9946 / 14.5k | 0.9971 | 0.9774 | 0.00696 | 25 min |
| hull 256^2 | 0.9933 / 6.5k / 1.9 % | 0.9948 / 16.0k | **0.9973** | **0.9809** | **0.00668** | 16 min |
| no hull | 0.9926 / 5.8k / 2.3 % | 0.9948 / 14.8k | 0.9972 | 0.9769 | 0.00688 | 14 min |

Conclusions:
1. On fertility (genus 4, correct) the hull field is irrelevant too: all three within 0.004 VolIoU
   / 0.0002 ho16, no-hull equals hull-512. Combined with 19: the hull matters only on rocker-arm
   (+0.04), where the thin plate's silhouettes leave a slab-thickness ambiguity that the hull resolves.
2. **New fertility headline (genus 4 = GT): ho16 0.997, VolIoU 0.977-0.981, ~15k V, SI 1.4-1.7 %.**
   VolIoU is below the old genus-5 number (0.986 at 9.4k V): the old chain's extra tube did not cost
   volume, and this base carries more residual tangles (SI 1.4 % vs 0.5 %). fertility still beats
   DMesh 0.979 only marginally (0.981 vs 0.979 at hull256) and Palfinger/Nicolet clearly.
3. The SI gate (5 %) must go or be local: SI rings are already excluded from splitting; the global
   gate only starves tangled bases. Default should become 0.3 (or gate on SI *growth*, not level).

## 20. armadillo: normal-map supervision + no Taubin (task arma_nrm, serial, 2026-09-07)
Boss: Palfinger's scales come from 3-channel normal supervision + no smoothing; test that on OUR shape
where it should matter. Start = golden pre-Taubin mesh `results64v/cow_armadillo_p6_50k.npz` (56.8k f),
+1200 in-loop steps (no adaptive remesh), evaluated RAW (no Taubin) and after AUTO Taubin.

| variant | raw ho16 / VolIoU / CD / SI | after Taubin ho16 / VolIoU / CD | wall |
|---|---|---|---|
| A control (LAP 1.0) | 0.9925 / 0.9848 / 0.00698 / 2.5 % | 0.9971 / **0.9912** / 0.00656 | 40 min |
| B W_NORMAL 0.3 | 0.9937 / 0.9858 / 0.00712 / 1.6 % | 0.9975 / 0.9912 / 0.00673 | 35 min |
| C W_NORMAL 0.3 + LAP 0.3 | 0.9935 / 0.9848 / 0.00709 / 1.9 % | 0.9973 / 0.9904 / 0.00673 | 32 min |
Palfinger (16b): 0.9965 / 0.9938. Peak VRAM 1.57 GB at 55k faces.

Conclusions:
1. Normals give +0.001 before Taubin and nothing after; weakening the Laplacian gives nothing. Taubin
   is not what erases the scales -- it RAISES VolIoU by 0.006 on every variant (removes jitter that the
   volume metric counts as error). So neither "no smoothing" nor "normal images" explains Palfinger's
   +0.003 VolIoU on armadillo; the remaining candidates are its per-step remeshing at ~1.5 px edges vs
   our fixed 56.8k-face mesh and its 3-channel normal loss on ALL pixels without a depth term.
2. Side result: 1200 more in-loop steps on the golden mesh lift armadillo VolIoU 0.9885 -> 0.9912
   (ho16 0.9972 -> 0.9971): the golden chain was under-trained for the volume metric. New armadillo
   headline: **0.9971 / 0.9912** (`results_genus/armadillo_ctrl_auto.npz`).
3. W_NORMAL stays off. The armadillo gap to Palfinger (0.9912 vs 0.9938) is now the only shape where
   we are not first, and it is a genus-0 accuracy question, not a topology one.

## 21. In-loop velocity-weighted Laplacian (W_VLAP, Palfinger core/opt.py style; task arma_vlap, 2026-09-07)
Boss: "why can't we smooth every step like Palfinger?" We could -- our W_LAP term is uniform, so it was
kept weak and the jitter was left for Taubin. `W_VLAP` (commit dd99f2b): per-vertex EMA displacement
-> relative velocity nu (median-normalised, clamp 5) -> loss += W_VLAP * mean(nu * |v - nbr_mean|^2) /
mean_edge^2. Same setup as 20 (golden p6_50k + 1200 steps, raw and after AUTO Taubin).

| W_VLAP | raw ho16 / VolIoU / CD / SI | Taubin ho16 / VolIoU / CD | wall |
|---|---|---|---|
| 0 (ctrl, 20) | 0.9925 / 0.9848 / 0.00698 / 2.5 % | 0.9971 / 0.9912 / 0.00656 | 40 min |
| 0.03 | 0.9969 / 0.9925 / 0.00632 / 0.0 % | **0.9979 / 0.9932 / 0.00628** | 31 min |
| 0.1 | **0.9976 / 0.9924** / 0.00632 / 0.0 % | 0.9978 / 0.9917 / 0.00633 | 43 min |
| 0.3 | 0.9972 / 0.9888 / 0.00649 / 0.0 % | 0.9970 / 0.9878 / 0.00654 | 31 min |
Palfinger: 0.9965 / 0.9938 / 0.00678 (76k V, 55.6k? no: 37.9k V).

Conclusions:
1. This was the missing piece on armadillo. Velocity-weighted smoothing removes the jitter DURING
   optimisation: raw VolIoU 0.9848 -> 0.9924, SI 2.5 % -> 0, and Taubin becomes almost a no-op
   (+0.0007 at 0.03, -0.0007 at 0.1). New armadillo headline **0.9979 / 0.9932 / CD 0.00628 / SI 0 %**
   (W_VLAP 0.03 + Taubin) -- within 0.0006 VolIoU of Palfinger and better on ho16 and Chamfer.
2. The sweet spot is small (0.03-0.1); 0.3 over-smooths (VolIoU drops to 0.989 raw). Uniform W_LAP=0.1
   never reached this because it cannot be weak on converged vertices and strong on moving ones.
3. Chamfer improves 0.00656 -> 0.00628: the jitter was a Chamfer cost too. Every earlier headline was
   paid for by post-hoc Taubin; W_VLAP=0.03 should become a default and the genus shapes re-run with it
   (expect SI 1.4-1.7 % on fertility -> ~0).

## 22. armadillo with Palfinger's optimizer parameters (task arma_palf, 2026-09-08)
Boss: "use Palfinger's parameters." Mapped 1:1 from `core/opt.py` (commit b7551bc): Adam betas
(0.8, 0.8); nu = |m1/sqrt(m2)| from the Adam state; grad += 0.02 * nu * (v - nbr_mean) (on the gradient,
not the loss); grad clip 10*|m1|; lr = 0.3 * mean edge (overrides cosine); velocity edge controller
(gain 0.2, ADAPT_MODE=velocity); edge floor 0.01 unit-sphere = 1.3 px (was 3 px); face cap 100k.
Only deviation: their remesh is every step; pure-Python DLFL at 55k faces costs ~3 min per pass, so
every 50 (P50) and every 100 (P100) steps. Start = golden p6_50k, 1200 steps, raw and after Taubin.

| variant | raw ho16 / VolIoU / CD / SI | Taubin ho16 / VolIoU / CD | V | wall |
|---|---|---|---|---|
| ours, our params + W_VLAP 0.03 (21) | 0.9969 / 0.9925 / 0.00632 / 0 % | 0.9979 / 0.9932 / 0.00628 | 27.7k | 31 min |
| **ours, Palfinger params, remesh/50** | 0.9978 / 0.9943 / 0.00623 / 0 % | **0.9983 / 0.9948 / 0.00620** | 49.8k | 71 min |
| ours, Palfinger params, remesh/100 | 0.9978 / 0.9943 / 0.00622 / 0 % | 0.9982 / 0.9948 / 0.00620 | 48.8k | 69 min |
| Palfinger 2022 (original code) | -- | 0.9965 / 0.9938 / 0.00678 | 37.9k | 3.6 min |

Conclusions:
1. With its own optimizer settings on our DLFL loop we pass Palfinger on armadillo on all three metrics
   (VolIoU 0.9948 vs 0.9938, ho16 0.9983 vs 0.9965, CD 0.00620 vs 0.00678), with SI 0 and no Taubin
   needed (raw 0.9943). New armadillo headline: **0.9983 / 0.9948 / 49.8k V**.
2. What did it: nu-weighted Laplacian on the gradient (removes jitter while moving), betas 0.8 (fast,
   local moments), lr tied to edge length, and above all the 1.3 px edge floor: V 27.7k -> 49.8k, i.e.
   the earlier gap was mostly vertex budget on the head/face (§20-21 pits +0.04-0.08 px).
3. Remesh cadence 50 vs 100 makes no difference here (0.9948 both) -> per-step remeshing is not what
   we were missing; the edge-length target is. This also caps the cost argument: 12 DLFL passes suffice.
4. Palfinger is still 20x faster (3.6 vs 70 min): pure-Python DLFL + 64 full-res views per step.

### Parameters used (exact, LESSONS 22 -> proposed new default "v9")
Optimizer (Palfinger `core/opt.py` mapped 1:1 onto `phase4_inloop.py`):
```
ADAM_BETAS=0.8,0.8      # Adam moments (was 0.9,0.999)
PALF_LAP=0.02           # grad += 0.02 * nu * (v - mean(neighbors)), nu = |m1/sqrt(m2)| per vertex
PALF_CLIP=10            # clip grad to 10*|m1|
LR_EDGE=0.3             # lr = 0.3 * mean edge length, every step (replaces cosine LR schedule)
```
Remeshing (DLFL, in-loop):
```
ADAPT_REMESH=1 ADAPT_MODE=velocity ADAPT_NU_GAIN=0.2   # ref_len *= 1 + (nu/nu_med - 1) * 0.2
ADAPT_LMIN_PX=1.3       # edge floor = Palfinger's 0.01 unit-sphere (was 3 px)  <- the main lever
ADAPT_MAX_F=100000      # face cap (was 60k)
ADAPT_SI_GATE=0.3       # global split gate (was 0.05; 19b)
COLLAPSE_EVERY=50       # remesh cadence (100 gives the same result)
FLIP_EVERY=25 COLLAPSE_RATIO=0.4 SI_PUSH=0.15
STEPS=1200
```
Unchanged: supervision (sil + depth x0.35 + diffuse x1 + hull W_T=20, 64 views @256^2), W_LAP 0.1,
W_NORMAL 0, W_VLAP 0 (superseded by PALF_LAP), AUTO Taubin afterwards (optional: raw is 0.9943).
Full command (armadillo, from the golden pre-Taubin mesh):
```
env MODE=64v SHAPE=armadillo TAG=armadillo_palf50 STEPS=1200 FLIP_EVERY=25 COLLAPSE_EVERY=50 COLLAPSE_RATIO=0.4 SI_PUSH=0.15 \
    ADAM_BETAS=0.8,0.8 PALF_LAP=0.02 PALF_CLIP=10 LR_EDGE=0.3 \
    ADAPT_REMESH=1 ADAPT_MODE=velocity ADAPT_NU_GAIN=0.2 ADAPT_LMIN_PX=1.3 ADAPT_MAX_F=100000 ADAPT_SI_GATE=0.3 \
    BASE_NPZ=results64v/cow_armadillo_p6_50k.npz python3 -u despike/phase4_inloop.py
env MODE=64v SHAPE=armadillo AUTO=1 TAG=armadillo_palf50_auto BASE_NPZ=.../cow_armadillo_armadillo_palf50.npz python3 despike/phase5_taubin.py
```
Not yet validated on the genus shapes (kitten / rocker-arm / fertility use ADAPT_MODE=residual, which
won on fertility in 15a; the combination residual-criterion + Palfinger optimizer + 1.3 px floor is the
next run). Result files: `results_genus/armadillo_palf50_{raw,auto}.npz`, renders `armadillo_palf_*.png`.

Ops (2026-09-09 22:26): WSL rebooted a 4th time (57 min uptime at 23:23) mid fertility Stage 6 of gif_v9_all; both bg tasks died silently. Relaunched (idempotent: only fertility Stage 6+ re-runs); 83 stale Stage-6 frames deleted so the GIF does not show the stage twice.

## 23. TopMod Catmull-Clark as C2F (C2F_SUBDIV=cc, task cc_c2f_armadillo, 2026-09-10) — PAUSED by Boss (GPU needed), killed at Stage 5 step ~30
Progress so far (armadillo, same uniform chain as gif_v9_all, only Stage 1/4 refinement differs):
| stage | midpoint (numpy, gif_v9_all) | Catmull-Clark (TopMod) |
|---|---|---|
| 1 cc3 (1.9k f) ho16 | (not logged) | 0.8872, hair 46k |
| 2 clean loop 400 ho16 | - | 0.9682 (SI 0.6 %) |
| 3 genus discovery | 0 handles | 0 handles |
| 4 cc4 + despike: train / ho16 / V | 0.9569 / 0.9847 / 3,450 | 0.9458 / 0.9820 / 5,108 (CC on triangles = 1->6, more faces) |
Outputs kept in out_liou: cow_armadillo_g3cccc3.npz, cow_armadillo_armadillo_g3cccc3p4.npz, cow_armadillo_g3cchlast.npz, cow_armadillo_g3ccearly.npz; 512 frames in out_liou/frames_cc_armadillo.
Resume: relaunch `bash despike/cc_c2f_chain.sh` (idempotent -> restarts at Stage 5). Early read: CC is slightly BEHIND midpoint after Stage 4 (ho16 0.9820 vs 0.9847) despite 48 % more vertices; Stage 5/6 decide.

RESULT (2026-09-10, resumed and finished): CC chain Stage 5 ho16 0.9957 / 17.9k V (midpoint 0.9956 / 18.1k); Stage 6 0.9979 / 49.0k (midpoint 0.9979 / 49.5k); Taubin x2 ho16 0.9982, VolIoU 0.9948 raw 0.9942, CD 0.00621 (midpoint: 0.9983 / 0.9949 / 0.9943 / 0.00620). => TopMod Catmull-Clark as the C2F refinement is EQUIVALENT to numpy midpoint after the golden v3 loops (difference within 1e-4); the early deficit at Stage 4 (0.9820 vs 0.9847) is erased by the adaptive remesh. Whole chain can be declared 100 % TopMod for topology with no loss. Meshes results_genus/armadillo_g3ccchain_{raw,auto}.npz, GIF gifs/armadillo_golden_v3_cc*.gif.

DECISION (2026-09-11, Boss): C2F_SUBDIV=cc is now the DEFAULT in run_64v.py; the quad->triangle step uses TopMod triangulate_all (DLFL insert_edge fan; unit test: identical triangle set to the old numpy fan). Every topology change in the chain is now a TopMod operator.
